//A module is like a separate file that contains code you can use in your program.
//It helps you organize your code into smaller, manageable pieces instead of keeping everything in one big file.
import{Circle} from './circle.js'
const c = new Circle(10);
c.draw();